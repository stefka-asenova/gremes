# don't export anything - Not sure these are used somewhere.

MLE1params<- function()
{
  x<- numeric(0)
  class(x)<- "MLE1params"
  return(x)
}



MLE2params<- function()
{
  x<- numeric(0)
  class(x)<- "MLE2params"
  return(x)
}
